# =============================================================================
# AuthorizationPolicy — L7 access control for productpage via Waypoint
# =============================================================================
# SOURCE:
#   Patched from service-mesh-3.2/mesh/ambient/authorizationpolicy.yaml
#
# FIELDS CHANGED FROM SNO:
#   metadata.name:      productpage-waypoint  → productpage-waypoint-a
#   metadata.namespace: bookinfo              → bookinfo-a
#   targetRefs.name:    productpage           → productpage (unchanged)
#   principals:         bookinfo-ingress/...  → bookinfo-ingress-a/...
#                       bookinfo/sa/*         → bookinfo-a/sa/*
#
# SERVICE ACCOUNT NAMING — SAIL OPERATOR LESSON:
#   Sail Operator appends "-istio" to the Gateway CR name for the SA.
#   Gateway CR name: bookinfo-gateway-a
#   Actual SA created: bookinfo-gateway-a-istio
#
#   Always verify with:
#     oc get serviceaccount -n bookinfo-ingress-a
#   before writing principals.
#
# SPIFFE identity:
#   cluster.local/ns/bookinfo-ingress-a/sa/bookinfo-gateway-a-istio
# =============================================================================
apiVersion: security.istio.io/v1
kind: AuthorizationPolicy
metadata:
  name: productpage-waypoint-a
  namespace: bookinfo-a
spec:
  targetRefs:
  - kind: Service
    group: ""
    name: productpage
  action: ALLOW
  rules:
  # Rule 1 — Allow tenant-a ingress gateway (external traffic)
  - from:
    - source:
        principals:
        - cluster.local/ns/bookinfo-ingress-a/sa/bookinfo-gateway-a-istio
    to:
    - operation:
        methods: ["GET"]
  # Rule 2 — Allow internal bookinfo-a mesh traffic
  - from:
    - source:
        principals:
        - cluster.local/ns/bookinfo-a/sa/*
    to:
    - operation:
        methods: ["GET"]
